# MyCoach — Progress Log

## Completed

### Phase 0: Foundation

- [x] **Project scaffolding** (commit 96d7a07)
  - pyproject.toml with dependencies (fastapi, sqlalchemy, alembic, pydantic, etc.)
  - Dev dependencies (pytest, pytest-asyncio, httpx, ruff, mypy)
  - Full directory structure per PRD Section 4
  - .env.example with placeholder env vars

- [x] **config.py with Pydantic Settings** (commit ce47409)
  - All settings groups: app, database, Garmin, Claude API, email, scheduler
  - MYCOACH_ env prefix, .env file support
  - Updated .env.example to match config field names

- [x] **database.py + main.py + test infrastructure** (commit fc7294f)
  - `database.py`: async SQLAlchemy engine, async_sessionmaker, DeclarativeBase, `get_db` FastAPI dependency
  - `main.py`: FastAPI app factory with lifespan (auto-creates tables), `GET /api/system/status` health check
  - `tests/conftest.py`: in-memory SQLite test engine, dependency override, auto-setup/teardown, async httpx client
  - `tests/test_api/test_system.py`: health check test (passing)

- [x] **Alembic async migrations setup**
  - Initialized Alembic with async template (`alembic init -t async`)
  - `alembic/env.py`: imports `Base.metadata` from `mycoach.database`, reads DB URL from `mycoach.config.Settings` dynamically
  - `render_as_batch=True` for SQLite ALTER TABLE compatibility
  - `alembic.ini`: removed hardcoded URL (driven by app config)
  - Verified: `alembic upgrade head` and `alembic revision --autogenerate` both work

- [x] **SQLAlchemy ORM models + Alembic migration**
  - 12 models across 8 files in `src/mycoach/models/`:
    - `user.py`: `User` — profile, fitness level, goals
    - `data_source.py`: `DataSourceConfig` — per-source config, credentials, sync status
    - `sport_profile.py`: `SportProfile` — per-sport skill level, goals, preferences, benchmarks
    - `availability.py`: `WeeklyAvailability` — time slots per day for upcoming week
    - `health.py`: `DailyHealthSnapshot` — HR, HRV, sleep, Body Battery, stress, training metrics, VO2max
    - `activity.py`: `Activity` + `GymWorkoutDetail` — workouts with HR/training effect; Hevy CSV sets/reps/weight/RPE
    - `plan.py`: `WeeklyPlan` + `PlannedSession` — generated plans with mesocycle tracking, linked to activities
    - `coaching.py`: `CoachingInsight` + `MesocycleConfig` — daily briefings, post-workout analysis, training blocks
    - `prompt_log.py`: `PromptLog` — LLM call tracking (tokens, latency, cost, prompt/response)
  - `models/__init__.py` re-exports all models
  - `main.py` and `alembic/env.py` import models to register with Base.metadata
  - Alembic autogenerate migration created and verified
  - All tests pass, ruff clean, mypy clean

- [x] **Pydantic schemas/DTOs in `schemas/`**
  - 9 schema files covering all 12 ORM models:
    - `user.py`: `UserCreate`, `UserUpdate`, `UserRead` with EmailStr validation
    - `data_source.py`: `DataSourceConfigCreate/Update/Read`, `DataSourceStatus`
    - `sport_profile.py`: `SportProfileCreate/Update/Read` with skill level validation
    - `availability.py`: `AvailabilitySlot`, `WeeklyAvailabilityCreate/Read`
    - `health.py`: `DailyHealthSnapshotCreate/Read` with all biometric fields
    - `activity.py`: `ActivityCreate/Read`, `GymWorkoutDetailCreate/Read` with RPE validation (1-10)
    - `plan.py`: `WeeklyPlanCreate/Read`, `PlannedSessionCreate/Read` with nested sessions
    - `coaching.py`: `CoachingInsightCreate/Read`, `MesocycleConfigCreate/Update/Read`
    - `system.py`: `StatusResponse`
  - `schemas/__init__.py` re-exports all 22 public schemas
  - Added `email-validator>=2.0.0` dependency for `EmailStr`
  - Validation: regex patterns for enums (fitness_level, skill_level, phase, insight_type, set_type), Field constraints (ge/le for day_of_week, rpe)
  - `from_attributes=True` on all Read schemas for ORM compatibility
  - 26 tests in `tests/test_schemas.py` covering valid/invalid cases
  - All tests pass, ruff clean, mypy clean

### Phase 1: Data Sources (in progress)

- [x] **Data source plugin system + Hevy CSV import**
  - `sources/base.py`: Abstract `DataSource` interface with `authenticate()`, `fetch_and_import()`, and `ImportResult` dataclass
  - `sources/hevy/csv_parser.py`: Parses Hevy CSV exports into `HevyWorkout`/`HevySet` dataclasses
    - Unit conversion: lbs→kg, miles→meters
    - Validates required columns, set_index, RPE range (1-10), datetime formats
    - BOM stripping for Windows-exported CSVs
    - Groups CSV rows into workouts by (title, start_time)
  - `sources/hevy/mappers.py`: Imports parsed workouts into DB as `Activity` + `GymWorkoutDetail` records
    - Deduplication by (user_id, title, start_time, data_source)
    - Duration calculation from start/end times
  - `api/routes/sources.py`: `POST /api/sources/import/hevy` endpoint (file upload)
  - Router registered in `main.py`
  - Added B008 ignore to ruff config (standard FastAPI `Depends()` pattern)
  - 20 new tests in `tests/test_sources/test_hevy.py`: CSV parsing (11), DB import (5), API endpoint (4)
  - All 47 tests pass, ruff clean, mypy clean

- [x] **Health + Activities API endpoints**
  - `api/routes/health.py`: 3 endpoints
    - `GET /api/health/today` — today's health snapshot (404 if none)
    - `GET /api/health/{date}` — snapshot for a specific date
    - `GET /api/health/trends?days=N` — last N days of snapshots (default 30, max 365)
  - `api/routes/activities.py`: 2 endpoints
    - `GET /api/activities` — paginated list with optional `sport` filter, includes gym workout details for gym activities
    - `GET /api/activities/{id}` — single activity with gym details
  - `PaginatedActivities` response model with `items`, `total`, `page`, `per_page`
  - Gym workout details loaded via join query (ordered by set_index)
  - Both routers registered in `main.py`
  - 15 new tests: health endpoints (7), activities endpoints (8)
  - All 62 tests pass, ruff clean, mypy clean

- [x] **Garmin source (auth, client, fetcher, mappers)**
  - `sources/garmin/auth.py`: `GarminAuth` class for authentication via garth
    - Token persistence: saves/resumes tokens from disk (configurable `garmin_token_dir`)
    - Fallback: resumes from saved tokens first, falls back to email/password login
    - Robust re-auth on token expiration
  - `sources/garmin/client.py`: `GarminClient` wrapper around garminconnect library
    - Methods for all health endpoints: stats, HR, HRV, sleep, stress, Body Battery, training readiness/status, VO2 max, respiration, SpO2
    - `get_activities_by_date()` for activity fetching
  - `sources/garmin/mappers.py`: Maps raw Garmin JSON to ORM models
    - `map_health_snapshot()`: Builds `DailyHealthSnapshot` from 10+ API responses, handles missing data gracefully, stores raw JSON for debugging
    - `map_activity()`: Maps Garmin activities to `Activity` model with sport classification (swimming, gym, cardio, other)
    - `import_health_snapshot()`: DB import with date-based deduplication
    - `import_activities()`: DB import with Garmin activity ID deduplication
    - Sport classification map: `GARMIN_SPORT_MAP` covering swimming variants, strength, cardio types
  - `sources/garmin/source.py`: `GarminSource` implements `DataSource` interface
    - `fetch_and_import()`: Orchestrates day-by-day health fetch + activity range fetch
    - `_safe_call()`: Wraps each API call so partial data is still captured on individual failures
    - Default 7-day lookback, configurable via `since` parameter
  - `api/routes/sources.py`: `POST /api/sources/sync/garmin` endpoint
    - Query param `days` (1-90, default 7) controls lookback period
    - Returns `GarminSyncResponse` with activities_created/skipped, health_snapshots_created, errors
    - 503 on auth failure with descriptive message
  - Enabled `garminconnect>=0.2.0` and `garth>=0.4.0` dependencies in pyproject.toml
  - 17 new tests in `tests/test_sources/test_garmin.py`:
    - Health snapshot mapper (3): full data, stats only, empty stats
    - Activity mapper (4): swimming, gym, unknown type, missing fields
    - DB import health (2): new + duplicate skip
    - DB import activities (3): new, duplicate skip, missing ID
    - GarminSource integration (2): fetch_and_import with mock client, auth failure
    - API endpoint (3): sync success, auth failure (503), days param
  - All 79 tests pass, ruff clean, mypy clean

- [x] **Data merging logic (Garmin + Hevy by date/time overlap)**
  - `sources/merger.py`: `merge_garmin_hevy()` finds overlapping gym activities by time
    - Matches Hevy gym activities with Garmin gym activities using time overlap with 30-min tolerance
    - Copies Garmin HR/calories/training effect data onto the Hevy activity
    - Sets `data_source` to "merged" and stores `garmin_activity_id`
    - Deletes redundant Garmin activity (frees unique constraint before update)
    - Picks best overlap when multiple Garmin activities exist
    - Fills missing Hevy duration from Garmin data
    - Idempotent: already-merged activities are skipped
  - `MergeResult` dataclass tracks merged count and errors
  - `api/routes/sources.py`: Updated existing endpoints + new endpoint
    - `POST /api/sources/merge`: Manual merge trigger
    - `POST /api/sources/import/hevy`: Now auto-merges after Hevy import
    - `POST /api/sources/sync/garmin`: Now auto-merges after Garmin sync
    - Response models updated with `activities_merged` field
  - 10 new tests in `tests/test_sources/test_merger.py`:
    - Merge logic (8): overlapping merge, no overlap, non-gym sport, no double merge, time tolerance, best overlap selection, no Hevy activities, duration fill from Garmin
    - API endpoint (2): merge endpoint success, nothing to merge
  - All 89 tests pass, ruff clean, mypy clean

- [x] **`GET /api/sources/status` endpoint** (Phase 1 complete)
  - `api/routes/sources.py`: New `GET /api/sources/status` endpoint
    - Returns status for each known source type (garmin, hevy_csv)
    - Derives status from actual data in DB (health snapshots, activities)
    - `sync_status`: "ok" when data exists, "never" when no data imported yet
    - `last_sync_at`: timestamp of most recently created record per source
    - "merged" activities count toward both garmin and hevy_csv status
  - `SourcesStatusResponse` wrapper model with `sources: list[DataSourceStatus]`
  - Uses existing `DataSourceStatus` schema from `schemas/data_source.py`
  - 4 new tests in `tests/test_sources/test_sources_status.py`:
    - No data (both "never"), garmin health data, hevy activity data, merged counts for both
  - All 93 tests pass, ruff clean, mypy clean

### Phase 2: Coaching Core (in progress)

- [x] **LLM client, prompt builder, response parser, coaching engine, daily briefing**
  - Enabled `anthropic>=0.40.0` dependency in pyproject.toml
  - `coaching/llm_client.py`: `LLMClient` wrapper around Anthropic Python SDK
    - `LLMResponse` dataclass with content, model, token counts, latency, cost
    - Cost estimation based on model pricing (Sonnet for daily, Opus for weekly)
    - Configurable model selection via `daily_model`/`weekly_model` properties
  - `coaching/response_parser.py`: JSON→Pydantic validation
    - `_extract_json()`: Handles markdown code blocks, raw JSON, surrounding text
    - `DailyBriefingResponse` model: sleep assessment, recovery status, readiness verdict (go_hard/moderate/active_recovery/rest), workout adjustments, sleep recommendation, key metrics
    - `parse_response()`: Generic JSON extraction + Pydantic validation with clear error messages
  - `coaching/prompt_builder.py`: Context assembly + prompt formatting
    - Loads versioned templates from `prompts/v1/`
    - `_format_health()`, `_format_health_trends()`, `_format_activities()`: Human-readable context formatting
    - `build_daily_briefing_prompt()`: Assembles full user message from health/activity data
    - `snapshot_to_dict()`, `activity_to_dict()`: ORM→dict converters
  - `coaching/context.py`: DB queries for prompt context
    - `get_today_health()`: Today's health snapshot
    - `get_health_trends()`: Last N days (excluding today)
    - `get_recent_activities()`: Recent activities by date range
  - `coaching/engine.py`: `CoachingEngine` orchestrator
    - `generate_daily_briefing()`: Full pipeline — context gather → prompt build → LLM call → parse → store
    - Retry once on JSON parse failure with explicit JSON instruction
    - Duplicate check (one briefing per day)
    - Stores `CoachingInsight` in DB + logs `PromptLog` (success and failure)
  - `prompts/v1/system.txt`: Coaching persona prompt (direct, data-driven, progressive)
  - `prompts/v1/daily_briefing.txt`: Daily briefing template with structured JSON output schema
  - `api/routes/coaching.py`: 2 endpoints
    - `GET /api/coaching/today` — retrieve today's briefing (404 if none)
    - `POST /api/coaching/today/generate` — generate briefing via LLM (409 if exists, 502 on failure)
  - Router registered in `main.py`
  - 34 new tests in `tests/test_coaching/`:
    - `test_response_parser.py` (10): JSON extraction (4), parse validation (6)
    - `test_prompt_builder.py` (10): format helpers (5), system prompt (1), build prompt (2+2)
    - `test_context.py` (7): health today (2), health trends (2), activities (2+1)
    - `test_engine.py` (5): success, prompt logging, duplicate check, LLM failure logging, retry on bad JSON
    - `test_api.py` (3): GET existing, 404 when none, 409 on duplicate generate
  - All 127 tests pass, ruff clean, mypy clean

### Phase 3: Weekly Plans (in progress)

- [x] **Availability API endpoints**
  - `api/routes/availability.py`: Full CRUD for weekly availability slots
    - `POST /api/availability`: Set weekly availability (replaces existing slots for that week)
    - `GET /api/availability/next-week`: Get slots for upcoming week (auto-calculates next Monday)
    - `GET /api/availability/{week_start}`: Get slots for a specific week (by Monday date)
    - `PUT /api/availability/{slot_id}`: Update a single slot
    - `DELETE /api/availability/{slot_id}`: Delete a single slot
  - Validation: `week_start` must be a Monday (422 if not)
  - Idempotent POST: deletes existing slots for the week before inserting new ones
  - Router registered in `main.py`
  - 10 new tests in `tests/test_api/test_availability.py`:
    - Set availability (1), replaces existing (1), rejects non-Monday (1)
    - Get week (1), rejects non-Monday (1), empty result (1)
    - Update slot (1), not found (1)
    - Delete slot (1), not found (1)
  - All 137 tests pass, ruff clean, mypy clean

- [x] **Weekly plan generation engine + API endpoints**
  - `coaching/response_parser.py`: Added `WeeklyPlanResponse` + `WeeklyPlanSessionResponse` models
    - Validates summary, sessions list (min 1), day_of_week (0-6), sport, title, duration, details (dict), notes
  - `coaching/context.py`: Added `get_availability_for_week()` — fetches availability slots for a given week with day names
  - `coaching/prompt_builder.py`: Added `_format_availability()` and `build_weekly_plan_prompt()`
    - Formats availability slots, health trends, recent activities, mesocycle context into prompt
  - `prompts/v1/weekly_plan.txt`: Weekly plan generation template
    - Instructions for sport-specific session generation (gym exercises/sets/reps/RPE, swimming drills/intervals, padel drills)
    - Mesocycle-aware (deload week volume/intensity reduction)
    - JSON output schema with sessions array matching availability slots
  - `coaching/engine.py`: Added `generate_weekly_plan()` method
    - Full pipeline: gather context → build prompt → LLM call (weekly model, 8192 max tokens) → parse → store
    - Validates week_start is Monday, checks for existing active plan (no duplicates)
    - Requires availability slots (raises ValueError if none)
    - Retry once on JSON parse failure
    - Stores `WeeklyPlan` + `PlannedSession` records, logs `PromptLog`
    - Session details stored as JSON in `PlannedSession.details`
  - `api/routes/plans.py`: 4 endpoints
    - `POST /api/plans/generate?week_start=` — generate plan via LLM (409 if exists/no availability, 502 on failure)
    - `GET /api/plans/current` — active plan for current week (404 if none)
    - `GET /api/plans/{plan_id}` — specific plan with sessions
    - `GET /api/plans/{plan_id}/sessions` — list sessions for a plan
  - Router registered in `main.py`
  - 20 new tests in `tests/test_plans/`:
    - `test_engine.py` (7): success, prompt logging, duplicate check, no availability, non-Monday, LLM failure, JSON details
    - `test_api.py` (7): get by id, not found, sessions list, sessions not found, no current, generate no availability 409, generate non-Monday 409
    - `test_context.py` (3): returns slots, empty, filters by week
    - `test_prompt_builder.py` (3): format slots, empty slots, build full prompt
  - All 157 tests pass, ruff clean, mypy clean

- [x] **Mesocycle tracking API + context integration**
  - `api/routes/mesocycles.py`: Full CRUD for mesocycle configurations per sport
    - `POST /api/mesocycles`: Create mesocycle config (409 if sport already exists)
    - `GET /api/mesocycles`: List all mesocycle configs
    - `GET /api/mesocycles/{sport}`: Get config for a specific sport
    - `PUT /api/mesocycles/{sport}`: Partial update (current_week, phase, progression_rules)
    - `DELETE /api/mesocycles/{sport}`: Delete mesocycle for a sport
  - `coaching/context.py`: Added `get_mesocycle_context()` function
    - Builds human-readable mesocycle summary from all configured sports
    - Detects deload weeks (current_week >= block_length_weeks) and flags them
    - Includes progression rules when configured
    - Returns None when no mesocycles configured (falls back to general programming)
  - `coaching/engine.py`: Updated `generate_weekly_plan()` to fetch and pass mesocycle context
    - Mesocycle data now flows into weekly plan LLM prompts via `mesocycle_context` parameter
  - Router registered in `main.py`
  - 14 new tests in `tests/test_api/test_mesocycles.py`:
    - API endpoints (10): create, duplicate (409), list, list empty, get by sport, not found, update, update not found, delete, delete not found
    - Context function (4): no configs, single sport, deload week detection, multiple sports
  - All 171 tests pass, ruff clean, mypy clean

### Phase 4: Post-Workout Analysis (in progress)

- [x] **Post-workout analysis engine + API endpoints**
  - `coaching/response_parser.py`: Added `PostWorkoutResponse` model
    - Validates performance_summary, planned_vs_actual, performance_trends, hr_analysis,
      training_effect_assessment, key_highlights (min 1), areas_for_improvement (min 1),
      next_session_recommendations, recovery_notes
  - `coaching/context.py`: Added 4 new context query functions
    - `get_activity_with_details()`: Fetches activity + gym workout details as dicts
    - `find_matching_planned_session()`: Matches activity to planned session by date + sport + week
    - `get_similar_activities()`: Gets last N activities of same sport for trend comparison
    - `link_activity_to_planned_session()`: Marks PlannedSession as completed + links activity_id
  - `coaching/prompt_builder.py`: Added 4 new formatting/building functions
    - `_format_activity_detail()`: Detailed single-activity formatting with all fields
    - `_format_gym_details()`: Formats sets/reps/weight/RPE grouped by exercise
    - `_format_planned_session()`: Formats planned session for comparison
    - `build_post_workout_prompt()`: Assembles full prompt from all context data
  - `prompts/v1/post_workout.txt`: Post-workout analysis template
    - Includes activity data, gym details, planned session comparison, similar activities, health context
    - JSON output schema with 9 fields covering performance, trends, HR, recommendations
  - `coaching/engine.py`: Added `generate_post_workout_analysis()` method
    - Full pipeline: gather context → match planned session → find similar activities → LLM call → parse → store
    - Duplicate check (one analysis per activity)
    - Auto-links activity to planned session when match found (marks completed)
    - Uses daily model (Sonnet) for cost efficiency
    - Retry once on JSON parse failure
    - Stores `CoachingInsight` (type=post_workout, linked to activity_id) + logs `PromptLog`
  - `api/routes/activities.py`: 2 new endpoints
    - `GET /api/activities/{id}/analysis` — retrieve post-workout analysis (404 if none)
    - `POST /api/activities/{id}/analyze` — trigger analysis via LLM (409 if exists, 502 on failure)
  - `api/routes/plans.py`: 1 new endpoint
    - `PATCH /api/plans/{plan_id}/sessions/{session_id}` — mark planned session as completed, optionally link to activity
  - 33 new tests in `tests/test_post_workout/`:
    - `test_context.py` (10): activity with details (3), find matching planned session (3), similar activities (2), link to planned session (1), not found (1)
    - `test_prompt_builder.py` (8): format activity detail (2), format gym details (3), format planned session (2), build full prompt (1)
    - `test_engine.py` (7): success, prompt logging, duplicate check, activity not found, LLM failure, planned session linking, swimming activity
    - `test_response_parser.py` (4): valid JSON, code block, missing field, empty highlights
    - `test_api.py` (4): get existing analysis, 404 no analysis, 404 nonexistent, mark session completed
  - All 204 tests pass, ruff clean, mypy clean

- [x] **Plan adherence tracking**
  - `schemas/plan.py`: Added `PlanAdherenceRead` + `SessionAdherenceRead` response models
    - `PlanAdherenceRead`: plan_id, week_start, total_sessions, completed_sessions, adherence_pct (0-100), sessions list
    - `SessionAdherenceRead`: session_id, day_of_week, sport, title, completed, activity_id
  - `api/routes/plans.py`: New `GET /api/plans/{plan_id}/adherence` endpoint
    - Returns adherence stats: total sessions, completed count, adherence percentage
    - Per-session breakdown with completion status
    - Sessions ordered by day_of_week
    - 404 when plan not found
  - `schemas/__init__.py`: Exported new schemas
  - 5 new tests in `tests/test_plans/test_api.py`:
    - None completed (0%), partial (66.7%), all completed (100%), plan not found (404), sessions ordered by day
  - All 209 tests pass, ruff clean, mypy clean

### Phase 5: Automation (in progress)

- [x] **Sleep coaching — prompt, engine, API, tests**
  - `prompts/v1/sleep_coaching.txt`: Sleep coaching prompt template
    - 14-day sleep trend analysis, recent activities, tomorrow's planned workout
    - JSON output schema with 9 fields: sleep quality summary, consistency analysis, sleep architecture,
      performance correlation, recommended bedtime/wake time, sleep debt assessment, hygiene tips (2-4), key concern
  - `coaching/response_parser.py`: Added `SleepCoachingResponse` model
    - Validates all 9 fields, hygiene_tips list length (min 2, max 4)
  - `coaching/context.py`: Added `get_sleep_trends()` function
    - Fetches sleep-focused health snapshots for last N days (default 14)
    - Returns sleep-specific fields (duration, score, deep/light/REM, Body Battery, HRV, stress, readiness)
  - `coaching/prompt_builder.py`: Added `_format_sleep_trends()` and `build_sleep_coaching_prompt()`
    - Formats sleep trend data with labeled fields per day
    - Assembles full prompt with sleep trends, recent activities, planned workout
  - `coaching/engine.py`: Added `generate_sleep_coaching()` method
    - Full pipeline: gather 14-day sleep trends + 7-day activities → build prompt → LLM call → parse → store
    - Duplicate check (one sleep coaching per day)
    - Uses daily model (Sonnet) for cost efficiency
    - Retry once on JSON parse failure
    - Stores `CoachingInsight` (type=sleep) + logs `PromptLog`
  - `api/routes/coaching.py`: 2 new endpoints
    - `GET /api/coaching/sleep` — retrieve today's sleep coaching (404 if none)
    - `POST /api/coaching/sleep/generate` — generate sleep coaching via LLM (409 if exists, 502 on failure)
  - 22 new tests in `tests/test_sleep_coaching/`:
    - `test_response_parser.py` (5): valid JSON, code block, missing field, too few tips, four tips
    - `test_context.py` (4): returns sleep data, empty, respects date range, ordered by date desc
    - `test_prompt_builder.py` (4): format entries, empty, multiple entries, build full prompt with planned workout
    - `test_engine.py` (5): success, prompt logging, duplicate check, LLM failure logging, works without data
    - `test_api.py` (3): get existing, 404 when none, 409 on duplicate generate
  - All 231 tests pass, ruff clean, mypy clean

- [x] **Weekly recap — prompt, engine, API, tests**
  - `prompts/v1/weekly_recap.txt`: Weekly recap prompt template
    - Week date range, plan adherence, activities, health trends, mesocycle status
    - JSON output schema with 8 fields: week summary, adherence analysis, performance highlights (2-4),
      areas of concern (1-3), recovery assessment, training load analysis, next week recommendations,
      mesocycle progress
  - `coaching/response_parser.py`: Added `WeeklyRecapResponse` model
    - Validates all 8 fields, performance_highlights list length (min 2, max 4),
      areas_of_concern list length (min 1, max 3)
  - `coaching/context.py`: Added 2 new context query functions
    - `get_plan_adherence_for_week()`: Fetches plan + sessions, calculates adherence percentage,
      returns per-session breakdown with completion status
    - `get_activities_for_week()`: Gets all activities within a specific week (Monday to Sunday)
  - `coaching/prompt_builder.py`: Added `_format_plan_adherence()` and `build_weekly_recap_prompt()`
    - Formats plan adherence with DONE/MISSED markers per session
    - Assembles full prompt with week dates, adherence, activities, health trends, mesocycle context
  - `coaching/engine.py`: Added `generate_weekly_recap()` method
    - Full pipeline: gather context → build prompt → LLM call → parse → store
    - Validates week_start is Monday, checks for existing recap (no duplicates)
    - Uses daily model (Sonnet) for cost efficiency
    - Retry once on JSON parse failure
    - Stores `CoachingInsight` (type=weekly_recap) + logs `PromptLog`
  - `api/routes/coaching.py`: 2 new endpoints
    - `GET /api/coaching/weekly-recap?week_start=` — retrieve weekly recap (defaults to last week, 404 if none)
    - `POST /api/coaching/weekly-recap/generate?week_start=` — generate recap via LLM (409 if exists, 422 non-Monday)
  - 26 new tests in `tests/test_weekly_recap/`:
    - `test_response_parser.py` (5): valid JSON, code block, missing field, too few highlights, too many concerns
    - `test_context.py` (5): adherence data, no plan, all completed, activities in range, empty week
    - `test_prompt_builder.py` (5): no plan, with adherence, empty sessions, full prompt, no plan no mesocycle
    - `test_engine.py` (6): success, prompt logging, duplicate check, non-Monday, LLM failure, with plan adherence
    - `test_api.py` (5): get existing, 404 when none, 422 non-Monday, 409 duplicate generate, 409 non-Monday generate
  - All 257 tests pass, ruff clean, mypy clean

- [x] **APScheduler background scheduler + daily pipeline jobs**
  - Enabled `apscheduler>=3.10.0` dependency in pyproject.toml
  - `scheduler/scheduler.py`: APScheduler `BackgroundScheduler` configuration
    - Creates scheduler with all 5 pipeline jobs configured via cron triggers
    - Configurable timezone, sync hour/minute, briefing hour/minute, weekly plan day/hour
    - `misfire_grace_time=3600` on all jobs (runs if up to 1 hour late)
    - `replace_existing=True` for safe restarts
    - Not auto-started: caller controls lifecycle
  - `scheduler/jobs.py`: 5 job functions for the full daily coaching pipeline
    - `job_garmin_sync()`: Syncs last 2 days of Garmin data + auto-merges with Hevy
    - `job_daily_briefing()`: Generates daily coaching briefing via LLM
    - `job_sleep_coaching()`: Generates sleep coaching analysis via LLM
    - `job_weekly_plan()`: Generates next week's training plan (calculates next Monday)
    - `job_weekly_recap()`: Generates recap for the previous week (calculates last Monday)
    - All jobs are idempotent — skip gracefully on duplicate (ValueError from engine)
    - Each job creates its own async DB session from `async_session`
    - `_run_async()` bridge for sync APScheduler → async job coroutines
  - `main.py`: Scheduler integrated into FastAPI lifespan
    - Starts scheduler on app startup (skipped when `env=test`)
    - Shuts down scheduler on app shutdown
    - `GET /api/system/scheduler`: New endpoint returning scheduler status
      - Running state, timezone, current time, list of jobs with next_run_time and trigger
  - `scheduler/__init__.py`: Exports `create_scheduler`
  - 15 new tests in `tests/test_scheduler/`:
    - `test_scheduler.py` (5): registers all jobs, configured timezone, default timezone, plan day configurable, not auto-started
    - `test_jobs.py` (9): garmin sync success/auth failure, daily briefing success/duplicate skip, sleep coaching success, weekly plan next Monday/from Sunday, weekly recap last Monday/duplicate skip
    - `test_api.py` (1): scheduler status endpoint when not running
  - All 272 tests pass, ruff clean, mypy clean

### Phase 6: PWA Frontend (in progress)

- [x] **Dashboard page + PWA infrastructure**
  - `templates/base.html`: Base layout with Tailwind CSS (CDN) + HTMX + responsive nav
    - Desktop top nav + mobile bottom nav with icons (Home, Plan, Schedule, History)
    - Active page highlighting, PWA manifest link, service worker registration
    - `hx-boost="true"` for SPA-like page transitions
  - `templates/dashboard.html`: Dashboard page extending base layout
    - Readiness card: color-coded by verdict (go_hard/moderate/active_recovery/rest), recovery status, adjustments
    - Health metrics grid: resting HR, HRV, Body Battery, sleep score, sleep duration, training readiness, steps, stress
    - Today's workout card: session title, sport, duration, notes, parsed JSON details (exercises, etc.)
    - Sleep recommendation card from daily briefing
    - Empty state messages for missing data (no health, no briefing, no plan, rest day)
  - `api/pages/dashboard.py`: Dashboard page route (`GET /`)
    - Fetches today's health snapshot, daily briefing (with JSON content parsing),
      current week's plan, and today's planned session (with details JSON parsing)
    - Passes structured data to Jinja2 template for server-side rendering
  - `main.py`: Updated to mount Jinja2 templates + static files + page router
    - `StaticFiles` mounted at `/static` from `src/mycoach/static/`
    - `Jinja2Templates` initialized from `src/mycoach/templates/`
    - Templates stored on `app.state.templates` for access in page routes
    - Dashboard page router registered
  - `static/manifest.json`: PWA manifest (standalone display, theme color, icon references)
  - `static/sw.js`: Service worker with network-first caching strategy, cache cleanup on activate
  - `static/js/app.js`: Service worker registration
  - `static/css/app.css`: Custom styles (HTMX transitions, mobile bottom nav spacer)
  - 6 new tests in `tests/test_pages/test_dashboard.py`:
    - Empty dashboard (1): renders HTML with empty state messages
    - Health metrics (1): displays resting HR, body battery, sleep score, formatted steps
    - Daily briefing (1): shows readiness verdict, recovery status, sleep recommendation
    - Plan with session (1): shows workout title, sport, duration, notes
    - Rest day (1): shows rest day message when plan exists but no session today
    - Static files (1): verifies CSS, JS, and manifest.json are served correctly
  - All 278 tests pass, ruff clean, mypy clean

- [x] **Plan view page**
  - `api/pages/plan.py`: Plan page route (`GET /plan`)
    - Fetches current week's active plan + all planned sessions
    - Builds full Mon-Sun week view with rest days for empty slots
    - Calculates adherence percentage (completed/total sessions)
    - Parses JSON details for each session
    - Highlights today's session, shows Done/Today badges
  - `templates/plan.html`: Plan page template extending base layout
    - Week date range header with adherence percentage
    - Empty state with link to availability page
    - Plan summary card (when available)
    - 7-day card list (Mon-Sun) with session details or "Rest day"
    - Today's card highlighted with blue accent
    - Done badge (green) for completed sessions, Today badge (blue) for current day
    - Expandable session details (exercises, notes) with key-value rendering
  - Router registered in `main.py`
  - 5 new tests in `tests/test_pages/test_plan.py`:
    - No plan (1): empty state with availability link
    - With sessions (1): shows all sessions, summary, details, rest days
    - Adherence (1): shows percentage and completed/total count
    - Today highlighted (1): shows Today badge for current day's session
    - Completed badge (1): shows Done badge for completed sessions
  - All 283 tests pass, ruff clean, mypy clean

- [x] **Availability input page**
  - `api/pages/availability.py`: Availability page route (`GET /availability`)
    - Fetches existing slots for next week, pre-fills form
    - Builds 7-day week view with dates for each day
    - Passes sport options for dropdown
  - `templates/availability.html`: Availability form template extending base layout
    - 7-day card list with toggle checkboxes for each day
    - Per-day inputs: start time, duration (30-120 min select), preferred sport
    - Pre-fills checked/time/duration/sport from existing slots
    - JavaScript form handler builds JSON payload and POSTs to `/api/availability`
    - Success/error feedback messages
  - Router registered in `main.py`
  - 5 new tests in `tests/test_pages/test_availability.py`:
    - Empty page (1): renders with all days, sports, and save button
    - Week dates (1): shows correct next week date range
    - Pre-fills existing (1): checked checkbox, time, duration for saved slot
    - Multiple slots (1): multiple checkboxes checked, unchecked days not checked
    - Save button (1): save button present
  - All 288 tests pass, ruff clean, mypy clean

- [x] **History page**
  - `api/pages/history.py`: History page route (`GET /history`)
    - Paginated activity list (20 per page) with sport filter
    - Fetches gym workout details for gym activities, groups by exercise
    - Checks which activities have post-workout analyses (Analyzed badge)
    - Groups activities by date for display
    - Available sports query for filter chips
  - `templates/history.html`: History page template extending base layout
    - Sport filter chips (All + each available sport) with active highlighting
    - Activities grouped by date with card layout
    - Activity cards: title, sport, time, duration, data source badge
    - Metrics row: avg/max HR, calories, training effect
    - Gym exercise details: exercise name with sets/reps/weight inline
    - Analyzed badge (purple) for activities with post-workout analysis
    - Pagination (Prev/Next) with page count
    - Empty state message
  - Router registered in `main.py`
  - 9 new tests in `tests/test_pages/test_history.py`:
    - Empty page (1): renders with empty state message and 0 count
    - With activities (1): shows titles, dates, grouped by date
    - Sport filter (1): filters to single sport, correct count
    - Gym details (1): shows exercise name, weight, reps
    - Metrics (1): shows HR and calories
    - Analysis badge (1): shows Analyzed badge for analyzed activities
    - Filter chips (1): shows sport filter links
    - Pagination (1): page 1/2 with Next, page 2/2 with Prev
    - Data source badge (1): shows data source label
  - All 297 tests pass, ruff clean, mypy clean

### Phase 7: Email (in progress)

- [x] **Email sender, templates, preferences, send triggers**
  - Enabled `resend>=2.0.0` and `aiosmtplib>=3.0.0` dependencies in pyproject.toml
  - `email/sender.py`: Email sender with dual backend support
    - `send_email()`: Dispatches to Resend API or SMTP based on config
    - `_send_via_resend()`: Sends via Resend API (preferred when API key configured)
    - `_send_via_smtp()`: Sends via SMTP with STARTTLS and optional auth
    - Convenience functions: `send_daily_briefing()`, `send_weekly_plan()`,
      `send_post_workout()`, `send_sleep_coaching()`, `send_weekly_recap()`
    - Each renders a Jinja2 email template then calls `send_email()`
    - Gracefully disabled when `email_enabled=False`
  - `email/templates/`: 6 Jinja2 email templates
    - `base.html`: Email layout wrapper (header, footer, card/badge/metric styles)
    - `daily_briefing.html`: Readiness verdict badge, sleep assessment, workout adjustments, key metrics
    - `weekly_plan.html`: Plan summary + per-session cards with sport badge, duration, details
    - `post_workout.html`: Performance summary, planned vs actual, HR analysis, highlights, improvements
    - `sleep_coaching.html`: Quality summary, consistency, architecture, schedule, hygiene tips
    - `weekly_recap.html`: Week summary, adherence, highlights, concerns, load analysis, mesocycle progress
  - `models/user.py`: Added 5 email preference columns to `User` model
    - `email_daily_briefing`, `email_weekly_plan`, `email_post_workout`,
      `email_sleep_coaching`, `email_weekly_recap` (all default `True`)
  - `schemas/user.py`: Added `EmailPreferencesRead` and `EmailPreferencesUpdate` schemas
    - `EmailPreferencesUpdate`: partial update with all fields optional
    - `UserRead`: now includes all 5 email preference fields
  - `api/routes/email_preferences.py`: 2 endpoints
    - `GET /api/email-preferences` — retrieve current email preferences (404 if no user)
    - `PATCH /api/email-preferences` — partial update of email preferences
  - `scheduler/jobs.py`: Email send triggers integrated into all coaching jobs
    - `_get_user_email_pref()`: Checks email_enabled config + per-user preference
    - Daily briefing, sleep coaching, weekly recap jobs: send email after LLM generation
    - Weekly plan job: fetches plan sessions, formats with day names, sends email
    - Email failures logged but don't block coaching generation
  - Alembic migration for email preference columns
  - Router registered in `main.py`
  - 24 new tests in `tests/test_email/`:
    - `test_sender.py` (19): template rendering (5), send_email function (5),
      convenience senders (5), backend selection (4)
    - `test_api.py` (5): get preferences, no user 404, update partial, update no user 404, empty update
    - `test_job_email.py` (4): daily briefing sends/skips email, sleep coaching sends,
      weekly recap sends
  - All 321 tests pass, ruff clean, mypy clean

### Phase 8: Polish & Hardening (in progress)

- [x] **Global exception handler in FastAPI**
  - `api/error_handlers.py`: Three exception handlers registered on the FastAPI app
    - `RequestValidationError` handler: Returns structured 422 JSON with per-field errors
      (`field` location path + `message`), logs warning
    - `HTTPException` handler: Returns consistent JSON structure (`error` + `detail`)
      for all HTTP errors (404, 409, 422, 502, 503, etc.)
    - Unhandled `Exception` handler: Catches all uncaught exceptions, returns generic 500
      JSON without leaking internal error details, logs full traceback via `logger.exception()`
  - `register_error_handlers()` function called in `create_app()` before router registration
  - All API error responses now have consistent JSON format: `{"error": "<type>", "detail": "<message>"}`
  - Validation errors include `"errors"` array with `field` and `message` per invalid input
  - 5 new tests in `tests/test_api/test_error_handlers.py`:
    - Structured 422 for validation errors (field + message)
    - Structured JSON for HTTPException (404)
    - Generic 500 without leaking error details (uses dedicated test app with `raise_app_exceptions=False`)
    - Exception logging verification (logger.exception called with method + path)
    - Multiple validation errors listed
  - All 326 tests pass, ruff clean, mypy clean

- [x] **Credential encryption (Fernet-based)**
  - Enabled `cryptography>=42.0.0` dependency in pyproject.toml
  - `crypto.py`: Fernet symmetric encryption utilities for credential storage
    - `generate_key()`: Generates a new Fernet encryption key for `MYCOACH_ENCRYPTION_KEY`
    - `encrypt_credentials()`: Encrypts a dict of credentials to a base64 Fernet token string
    - `decrypt_credentials()`: Decrypts back to a dict, raises `InvalidToken` on wrong key/corruption
  - `config.py`: Added `encryption_key` setting (`MYCOACH_ENCRYPTION_KEY` env var)
  - `schemas/data_source.py`: Added `CredentialsInput` and `CredentialsStored` schemas
    - `CredentialsInput`: Accepts plaintext `credentials: dict[str, str]` for API input
    - `CredentialsStored`: Response confirming credentials were encrypted and stored
    - `DataSourceConfigRead`: Added `has_credentials` field
  - `api/routes/credentials.py`: 3 endpoints for credential management
    - `PUT /api/credentials/{source_type}`: Encrypt and store credentials (creates/updates DataSourceConfig)
    - `DELETE /api/credentials/{source_type}`: Remove stored credentials
    - `GET /api/credentials/{source_type}/status`: Check if credentials exist (without revealing them)
    - Returns 503 when encryption key not configured
  - Router registered in `main.py`
  - `.env.example`: Added `MYCOACH_ENCRYPTION_KEY` with generation instructions
  - 16 new tests in `tests/test_crypto/`:
    - `test_crypto.py` (8): key generation (2), encrypt/decrypt roundtrip, not plaintext,
      wrong key raises, corrupted data raises, empty dict, nested values
    - `test_api.py` (8): store credentials, update existing, no encryption key (503),
      delete credentials, delete not found (404), status with credentials, status without,
      verify DB value is encrypted and decryptable
  - All 342 tests pass, ruff clean, mypy clean

- [x] **Structured logging (JSON formatter + request middleware)**
  - `logging_config.py`: Centralized logging configuration module
    - `JSONFormatter`: Formats log records as single-line JSON objects with timestamp, level,
      logger name, message, and optional fields (exception, method, path, status_code, duration_ms)
    - `setup_logging()`: Configures root logger with JSON handler to stderr, uses `log_level`
      setting from config (was previously defined but unused)
    - Quietens noisy third-party loggers (uvicorn.access, apscheduler) to WARNING level
  - `main.py`: Logging integration
    - `setup_logging(settings.log_level)` called at app startup in `create_app()`
    - `RequestLoggingMiddleware`: Logs every HTTP request with method, path, status code,
      and duration in milliseconds via structured JSON fields
    - Middleware registered before error handlers
  - 11 new tests in `tests/test_logging/test_logging_config.py`:
    - `TestJSONFormatter` (4): basic format, exception included, extra fields propagated,
      single-line output
    - `TestSetupLogging` (5): sets root level, adds JSON handler, case insensitive,
      quietens noisy loggers, replaces existing handlers
    - `TestRequestLoggingMiddleware` (2): logs request method/path, logs status code
  - All 353 tests pass, ruff clean, mypy clean

- [x] **Docker deployment (Dockerfile + docker-compose)**
  - `Dockerfile`: Multi-stage build with Python 3.11-slim + uv
    - Installs dependencies via `uv sync --no-dev` for production
    - Copies app code, alembic config, and migrations
    - Creates `/data` directory for SQLite DB and Garmin tokens
    - Default env vars for DB path and token dir within container
    - CMD runs Alembic migrations then starts uvicorn on port 8000
  - `docker-compose.yml`: Single-service setup with persistent volume
    - `app` service: builds from Dockerfile, reads `.env`, mounts `app-data` volume
    - Commented-out Caddy reverse proxy service for optional HTTPS
    - Named volumes for data persistence across container restarts
  - `.dockerignore`: Excludes `.git`, tests, docs, cache dirs, `.env`, DB files
  - `Caddyfile.example`: Template for Caddy reverse proxy configuration
  - All 353 tests pass, ruff clean, mypy clean

- [x] **User Profile CRUD API**
  - `api/routes/profile.py`: 3 endpoints for user profile management
    - `GET /api/profile` — retrieve user profile (404 if none)
    - `POST /api/profile` — create user profile (409 if exists, 201 on success)
    - `PUT /api/profile` — partial update of user profile (404 if none)
  - Single-user MVP: hardcoded `USER_ID = 1`
  - Uses existing `UserCreate`, `UserUpdate`, `UserRead` schemas
  - Default fitness_level "intermediate", email preferences all True
  - Router registered in `main.py`
  - 10 new tests in `tests/test_api/test_profile.py`:
    - Get profile (1), not found (1), create (1), duplicate (1),
      default fitness level (1), invalid fitness level (1),
      update (1), update not found (1), empty update (1),
      email prefs included (1)
  - All 363 tests pass, ruff clean, mypy clean

- [x] **Sport Profile CRUD API**
  - `api/routes/sport_profiles.py`: Full CRUD for per-sport skill levels, goals, and benchmarks
    - `POST /api/sport-profiles`: Create sport profile (409 if sport already exists)
    - `GET /api/sport-profiles`: List all sport profiles (ordered by sport name)
    - `GET /api/sport-profiles/{sport}`: Get profile for a specific sport
    - `PUT /api/sport-profiles/{sport}`: Partial update (skill_level, goals, preferences, benchmarks)
    - `DELETE /api/sport-profiles/{sport}`: Delete sport profile
  - Single-user MVP: hardcoded `USER_ID = 1`
  - Uses existing `SportProfile` model and `SportProfileCreate/Update/Read` schemas
  - Router registered in `main.py`
  - 12 new tests in `tests/test_api/test_sport_profiles.py`:
    - Create (1), duplicate (1), invalid skill level (1), list (1), list empty (1),
      get by sport (1), not found (1), update (1), update not found (1),
      delete (1), delete not found (1), default skill level (1)
  - All 375 tests pass, ruff clean, mypy clean

## Next Up

- [ ] Phase 8 (continued): testing
